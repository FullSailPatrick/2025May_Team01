name: Commit feed to Discord

on:
  push:
    branches:
      - "**"

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Post to Discord
        run: |
          curl -H "Content-Type: application/json" \
            -X POST \
            -d '{"content":"✅ GitHub push detected on `${{ github.repository }}` by `${{ github.actor }}` on branch `${{ github.ref }}`"}' \
            https://discord.com/api/webhooks/1370268101391810570/jZzmrKgUiw0U-1dfV8NXTtuRuFyHEBSPRu8TViDcYPOaEOWlt27rMRse5DQ4PdY5nXOD
